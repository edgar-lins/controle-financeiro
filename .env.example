# Ambiente (development, production)
ENVIRONMENT=development

# Configuração do Banco de Dados (Desenvolvimento)
DB_HOST=localhost
DB_PORT=5432
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=controle_financeiro

# Configuração do Banco de Dados (Produção - Render/Heroku)
# DATABASE_URL=postgresql://usuario:senha@host:porta/dbname

# Servidor
PORT=8080

# JWT Secret (IMPORTANTE: Use uma chave forte em produção!)
JWT_SECRET=seu_jwt_secret_muito_seguro_aqui

# CORS - Origens permitidas (separadas por vírgula)
# Desenvolvimento: http://localhost:5173
# Produção: https://seu-dominio.vercel.app
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
